{
  "name": "dynamo-s3-document-client",
  "version": "1.0.3",
  "author": "David Revay",
  "license": "ISC",
  "description": "Just like AWS DynamoDB DocumentClient but saves oversize files in S3.",
  "main": "build/index.js",
  "scripts": {
    "test": "jest src",
    "build": "rm -rf build && mkdir build && yarn tsc",
    "build:link": "yarn build && cd build && yarn link",
    "lint": "tslint --project tsconfig.lint.json"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/MrBlenny/dynamo-s3-document-client"
  },
  "dependencies": {
    "aws-sdk": "2.205.0",
    "lodash": "^4.17.5"
  },
  "devDependencies": {
    "@types/jest": "^22.2.2",
    "@types/lodash": "^4.14.106",
    "@types/node": "^9.6.4",
    "aws-sdk-mock": "^1.7.0",
    "jest": "^22.4.3",
    "np": "^2.20.1",
    "ts-jest": "^22.4.2",
    "tslint": "^5.9.1",
    "typescript": "2.6"
  }
}
